---
galaxy_info:
  author: Almas
  description: Base role for Ubuntu VMs (packages, timezone, locale, chrony, sysctl, motd, preflight)
  company: YourCompany
  
  min_ansible_version: "2.18"
  
  platforms:
    - name: Ubuntu
      versions:
        - "24.04"
        - "22.04"

  galaxy_tags:
    - base
    - ubuntu
    - system
    - preflight
