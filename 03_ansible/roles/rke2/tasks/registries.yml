---
- name: Deploy RKE2 registries configuration
  ansible.builtin.template:
    src: registries.yaml.j2
    dest: /etc/rancher/rke2/registries.yaml
    mode: '0600'
    owner: root
    group: root
  notify: Restart "{{ rke2_service_name }}"
