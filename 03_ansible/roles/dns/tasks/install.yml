---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install BIND9 and utilities
  ansible.builtin.apt:
    name: "{{ dns_packages }}"
    state: present
