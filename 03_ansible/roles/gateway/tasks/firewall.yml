---
# Ставим права iptables
- name: Deploy iptables rules
  ansible.builtin.template:
    src: rules.v4.j2
    dest: /etc/iptables/rules.v4
    owner: root
    group: root
    mode: "0600"
    validate: "iptables-restore --test %s"
  notify: Apply firewall


