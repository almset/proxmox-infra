---
- name: Setup Ansible Controller Prerequisites
  hosts: k8s_control_plane
# hosts: localhost
# connection: local
  become: true
  gather_facts: true

  roles:
    - role: controller/prerequisites
